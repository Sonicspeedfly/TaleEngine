# --- Веб-сервер ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9      # для загрузки файлов (импорт карточек)

# --- LLM шлюз ---
litellm>=1.40

# --- Документы (Word -> текст/PDF; экспорт канваса в Docx/PDF) ---
python-docx>=1.1            # чтение/создание .docx
fpdf2>=2.7                  # генерация PDF (экспорт канваса; кириллица через системный шрифт)

# --- База данных ---
sqlalchemy>=2.0
aiosqlite>=0.19              # асинхронный драйвер SQLite

# --- Настройки / валидация ---
pydantic>=2.6
pydantic-settings>=2.2

# --- Часовые пояса (zoneinfo на Windows требует базу tzdata) ---
tzdata>=2024.1

# --- Telegram-бот (общий образ с backend) ---
aiogram>=3.4

# --- Тесты ---
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27                  # тест-клиент для FastAPI
